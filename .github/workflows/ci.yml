on: [push]

name: CI

jobs:
  test:
    name: Test ${{ matrix.rust }}
    strategy:
      matrix:
        rust:
          - stable
          - beta
          - nightly
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install Rust (${{ matrix.rust }})
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          profile: minimal
          override: true

      - name: Async/Sync Feature 
        uses: actions-rs/cargo@v1
        env:
          RUST_LOG: trace
        run: |
          cargo test --test form-data --test hyper-body
          cargo test --test tiny-body --no-default-features --features="sync"
